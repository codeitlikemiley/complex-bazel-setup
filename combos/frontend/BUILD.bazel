load("@crates//:defs.bzl", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "frontend_bin",
    srcs = ["src/main.rs"],
    deps = all_crate_deps(normal = True),
    edition = "2024",
)

# No rust_test target: src/main.rs is a hello-world with nothing to assert. Its
# previous test wrapped two `assert!(true)` calls, which clippy rejects and which
# reported PASSED regardless of the code. Add one back with the first real test.
