# Cargo output directory. With a single workspace there is one target/ at the
# root, no matter which member directory cargo is invoked from. Bazel's package
# loader would otherwise crawl every file in it.
target
